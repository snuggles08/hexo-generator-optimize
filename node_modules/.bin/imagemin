#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../imagemin/bin/imagemin" "$@"
  ret=$?
else 
  node  "$basedir/../imagemin/bin/imagemin" "$@"
  ret=$?
fi
exit $ret
